# The PIPE -- The Pipe InterProcess communication Engine

This is the PIPE, a framework for connecting processes via pipes into a graph. The PIPE consists of three parts:

 * Functions that are used to implement a PIPE host
 * Functions that are used to implement PIPE clients
 * Common functionality such as protocols and a OS services abstraction layer
